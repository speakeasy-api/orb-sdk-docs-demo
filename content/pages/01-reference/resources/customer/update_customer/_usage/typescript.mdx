```typescript UpdateCustomer.ts
import { Orb } from "orb-billing";
import { UpdateCustomerRequest } from "orb-billing/dist/sdk/models/operations";
import {
  AccountingProviderConfig,
  AddressInput,
  CustomerTaxId,
  CustomerTaxIdCountry,
  CustomerTaxIdType,
  EditCustomer,
  EditCustomerMetadata,
  EditCustomerPaymentProvider,
  NewAccountingSyncConfiguration,
  NewReportingConfiguration,
} from "orb-billing/dist/sdk/models/shared";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const customerId: string = "string";
const editCustomer: EditCustomer = {
  accountingSyncConfiguration: {
    accountingProviders: [
      {
        externalProviderId: "string",
        providerType: "string",
      },
    ],
  },
  additionalEmails: [
    "string",
  ],
  billingAddress: {},
  metadata: {},
  reportingConfiguration: {
    exempt: false,
  },
  shippingAddress: {},
  taxId: {
    country: CustomerTaxIdCountry.Ro,
    type: CustomerTaxIdType.SaVat,
    value: "string",
  },
};

  const res = await sdk.customer.updateCustomer(customerId, editCustomer);


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
