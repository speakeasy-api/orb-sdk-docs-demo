```python Create.py
import orb
from orb.models import shared

s = orb.Orb(
    api_key_auth="",
)

req = shared.NewCustomer(
    accounting_sync_configuration=shared.NewAccountingSyncConfiguration(
        accounting_providers=[
            shared.AccountingProviderConfig(
                external_provider_id='string',
                provider_type='string',
            ),
        ],
    ),
    additional_emails=[
        'string',
    ],
    billing_address=shared.AddressInput(),
    email='Anahi38@hotmail.com',
    metadata=shared.NewCustomerMetadata(),
    name='string',
    reporting_configuration=shared.NewReportingConfiguration(
        exempt=False,
    ),
    shipping_address=shared.AddressInput(),
    tax_id=shared.CustomerTaxID(
        country=shared.CustomerTaxIDCountry.IN,
        type=shared.CustomerTaxIDType.RU_KPP,
        value='string',
    ),
)

res = s.customer.create(req)

if res.customer is not None:
    # handle response
    pass
```
