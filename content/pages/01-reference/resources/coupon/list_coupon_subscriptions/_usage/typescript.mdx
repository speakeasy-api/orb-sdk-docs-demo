```typescript ListCouponSubscriptions.ts
import { Orb } from "orb-billing";
import { ListCouponSubscriptionsRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const couponId: string = "string";
const cursor: string = "string";
const limit: number = 592845;

  const res = await sdk.coupon.listSubscriptions(couponId, cursor, limit);

  if (res.statusCode == 200) {
    do {
      // handle items

      res = res.next();
    } while (res);
  }
})();
```
