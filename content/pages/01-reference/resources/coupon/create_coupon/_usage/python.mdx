```python CreateCoupon.py
import orb
from orb.models import shared

s = orb.Orb(
    api_key_auth="",
)

req = shared.NewCoupon1(
    shared.PercentageDiscount(
        applies_to_price_ids=[
            'h74gfhdjvn7ujokd',
            '7hfgtgjnbvc3ujkl',
        ],
        discount_type=shared.PercentageDiscountDiscountType.PERCENTAGE,
        percentage_discount=0.15,
    ),
    duration_in_months=12,
    redemption_code='HALFOFF',
)

res = s.coupon.create(req)

if res.coupon is not None:
    # handle response
    pass
```
