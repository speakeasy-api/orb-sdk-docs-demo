```python ArchiveCoupon.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.coupon.archive(coupon_id='string')

if res.coupon is not None:
    # handle response
    pass
```
