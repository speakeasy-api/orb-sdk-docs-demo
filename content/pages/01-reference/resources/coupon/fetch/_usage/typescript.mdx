```typescript Fetch.ts
import { Orb } from "orb-billing";
import { FetchCouponRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const couponId: string = "string";

  const res = await sdk.coupon.fetch(couponId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
