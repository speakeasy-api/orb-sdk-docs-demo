```typescript FetchBalanceByExternalId.ts
import { Orb } from "orb-billing";
import { FetchCustomerCreditsExternalIdRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const externalCustomerId: string = "string";
const cursor: string = "string";
const limit: number = 844266;

  const res = await sdk.credit.fetchBalanceByExternalId(externalCustomerId, cursor, limit);


  if (res.statusCode == 200) {
    do {
      // handle items

      res = res.next();
    } while (res);
  }
})();
```
