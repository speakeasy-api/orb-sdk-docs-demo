```python FetchItem.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.item.fetch(item_id='string')

if res.item is not None:
    # handle response
    pass
```
