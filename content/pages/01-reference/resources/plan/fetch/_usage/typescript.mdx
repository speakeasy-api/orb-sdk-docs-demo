```typescript Fetch.ts
import { Orb } from "orb-billing";
import { FetchPlanRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const planId: string = "string";

  const res = await sdk.plan.fetch(planId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
