```python UnscheduleCancellation.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.subscription.unschedule_cancellation(subscription_id='string')

if res.subscription is not None:
    # handle response
    pass
```
