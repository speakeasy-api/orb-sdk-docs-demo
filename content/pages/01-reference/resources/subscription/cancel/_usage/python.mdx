```python Cancel.py
import orb
import dateutil.parser
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.subscription.cancel(subscription_id='string', cancel_subscription_params=shared.CancelSubscriptionParams(
    cancel_option=shared.CancelSubscriptionParamsCancelOption.IMMEDIATE,
))

if res.subscription is not None:
    # handle response
    pass
```
