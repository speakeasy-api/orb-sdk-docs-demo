```python CloseBackfill.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.event.close_backfill(backfill_id='string')

if res.backfill is not None:
    # handle response
    pass
```
