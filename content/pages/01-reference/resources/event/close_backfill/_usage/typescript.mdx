```typescript CloseBackfill.ts
import { Orb } from "orb-billing";
import { CloseBackfillRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const backfillId: string = "string";

  const res = await sdk.event.closeBackfill(backfillId);


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
