```typescript DeprecateEvent.ts
import { Orb } from "orb-billing";
import { DeprecateEventRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const eventId: string = "string";

  const res = await sdk.event.deprecateEvent(eventId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
