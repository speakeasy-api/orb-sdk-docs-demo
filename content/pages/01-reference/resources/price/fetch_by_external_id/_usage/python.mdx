```python FetchByExternalId.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.price.fetch_by_external_id(external_price_id='string')

if res.price is not None:
    # handle response
    pass
```
