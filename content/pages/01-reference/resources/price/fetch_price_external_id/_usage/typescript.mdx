```typescript FetchPriceExternalId.ts
import { Orb } from "orb-billing";
import { FetchPriceExternalIdRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const externalPriceId: string = "string";

  const res = await sdk.price.fetchByExternalId(externalPriceId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
