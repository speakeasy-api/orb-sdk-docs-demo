```typescript Create.ts
import { Orb } from "orb-billing";
import { Country, CustomerTaxIdType, NewCustomerPaymentProvider } from "orb-billing/dist/sdk/models/shared";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });

  const res = await sdk.customer.create({
    accountingSyncConfiguration: {
      accountingProviders: [
        {
          externalProviderId: "string",
          providerType: "string",
        },
      ],
    },
    additionalEmails: [
      "string",
    ],
    billingAddress: {},
    email: "Luna.Hoppe@yahoo.com",
    metadata: {},
    name: "string",
    reportingConfiguration: {
      exempt: false,
    },
    shippingAddress: {},
    taxId: {
      country: Country.Sk,
      type: CustomerTaxIdType.AuAbn,
      value: "string",
    },
  });


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
