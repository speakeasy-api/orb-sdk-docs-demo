```typescript Cancel.ts
import { Orb } from "orb-billing";
import { CancelSubscriptionRequest } from "orb-billing/dist/sdk/models/operations";
import { CancelOption, CancelSubscriptionParams } from "orb-billing/dist/sdk/models/shared";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const subscriptionId: string = "string";
const cancelSubscriptionParams: CancelSubscriptionParams = {
  cancelOption: CancelOption.Immediate,
};

  const res = await sdk.subscription.cancel(subscriptionId, cancelSubscriptionParams);


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
