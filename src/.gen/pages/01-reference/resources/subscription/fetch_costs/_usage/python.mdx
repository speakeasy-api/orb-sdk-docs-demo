```python FetchCosts.py
import orb
import dateutil.parser
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.subscription.fetch_costs(subscription_id='string', group_by='string', timeframe_end=dateutil.parser.isoparse('2022-03-01T05:00:00Z'), timeframe_start=dateutil.parser.isoparse('2022-02-01T05:00:00Z'), view_mode=operations.FetchSubscriptionCostsQueryParamViewMode.CUMULATIVE)

if res.subscription_costs is not None:
    # handle response
    pass
```
