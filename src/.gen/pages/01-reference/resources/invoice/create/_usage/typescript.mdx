```typescript Create.ts
import { Orb } from "orb-billing";
import { ModelType } from "orb-billing/dist/sdk/models/shared";
import { RFCDate } from "orb-billing/dist/sdk/types";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });

  const res = await sdk.invoice.create({
    currency: "USD",
    customerId: "4khy3nwzktxv7",
    externalCustomerId: "external-customer-id",
    invoiceDate: new Date("2023-09-22"),
    lineItems: [
      {
        endDate: new RFCDate("2023-09-22"),
        itemId: "4khy3nwzktxv7",
        modelType: ModelType.Unit,
        name: "Line Item Name",
        quantity: 1,
        startDate: new RFCDate("2023-09-22"),
        unitConfig: {
          unitAmount: "string",
        },
      },
    ],
    memo: "An optional memo for my invoice.",
    netTerms: 0,
    willAutoIssue: false,
  });


  if (res.statusCode == 200) {
    // handle response
  }
})();
```
