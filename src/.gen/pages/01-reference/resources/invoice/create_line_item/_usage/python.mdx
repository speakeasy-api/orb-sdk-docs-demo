```python CreateLineItem.py
import orb
import dateutil.parser
from orb.models import shared

s = orb.Orb(
    api_key_auth="",
)

req = shared.CreateInvoiceLineItemParams(
    amount='12.00',
    end_date=dateutil.parser.parse('2023-09-22').date(),
    invoice_id='4khy3nwzktxv7',
    name='Item Name',
    quantity=1,
    start_date=dateutil.parser.parse('2023-09-22').date(),
)

res = s.invoice.create_line_item(req)

if res.invoice_line_item is not None:
    # handle response
    pass
```
