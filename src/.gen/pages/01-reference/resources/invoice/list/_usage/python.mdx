```python List.py
import orb
import dateutil.parser
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)

req = operations.ListInvoicesRequest(
    statuses=[
        operations.QueryParamStatus.SYNCED,
    ],
)

res = s.invoice.list(req)

if res.invoices is not None:
    while True:
        # handle items

        res = res.Next()
        if res is None:
            break

```
