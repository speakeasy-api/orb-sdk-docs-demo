```typescript List.ts
import { Orb } from "orb-billing";
import { DateType, QueryParamStatus, Status } from "orb-billing/dist/sdk/models/operations";
import { RFCDate } from "orb-billing/dist/sdk/types";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });

  const res = await sdk.invoice.list({
    statuses: [
      QueryParamStatus.Issued,
    ],
  });


  if (res.statusCode == 200) {
    do {
      // handle items

      res = res.next();
    } while (res);
  }
})();
```
