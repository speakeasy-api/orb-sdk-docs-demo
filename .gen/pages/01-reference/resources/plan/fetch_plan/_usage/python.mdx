```python FetchPlan.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.plan.fetch(plan_id='string')

if res.plan is not None:
    # handle response
    pass
```
