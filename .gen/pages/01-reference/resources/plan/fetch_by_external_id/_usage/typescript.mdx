```typescript FetchByExternalId.ts
import { Orb } from "orb-billing";
import { FetchPlanExternalIdRequest } from "orb-billing/dist/sdk/models/operations";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const externalPlanId: string = "string";

  const res = await sdk.plan.fetchByExternalId(externalPlanId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
