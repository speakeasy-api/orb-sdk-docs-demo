```python Create.py
import orb
from orb.models import shared

s = orb.Orb(
    api_key_auth="",
)

req = shared.NewPlan(
    currency='Palladium',
    metadata=shared.NewPlanMetadata(),
    name='string',
    prices=[
        shared.NewPlanTieredPackagePrice(
            cadence=shared.NewPlanTieredPackagePriceCadence.QUARTERLY,
            item_id='string',
            model_type=shared.NewPlanTieredPackagePriceModelType.TIERED_PACKAGE,
            name='Annual fee',
            tiered_package_config={
                "key": 'string',
            },
        ),
    ],
)

res = s.plan.create(req)

if res.plan is not None:
    # handle response
    pass
```
