```python VoidInvoice.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.invoice.void(invoice_id='string')

if res.invoice is not None:
    # handle response
    pass
```
