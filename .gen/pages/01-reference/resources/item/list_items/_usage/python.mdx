```python ListItems.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.item.list(cursor='string', limit=118274)

if res.items is not None:
    while True:
        # handle items

        res = res.Next()
        if res is None:
            break

```
