```typescript UpdateByExternalId.ts
import { Orb } from "orb-billing";
import { UpdateCustomerExternalIdRequest } from "orb-billing/dist/sdk/models/operations";
import {
  AccountingProviderConfig,
  AddressInput,
  CustomerTaxId,
  CustomerTaxIdCountry,
  CustomerTaxIdType,
  EditCustomer,
  EditCustomerMetadata,
  EditCustomerPaymentProvider,
  NewAccountingSyncConfiguration,
  NewReportingConfiguration,
} from "orb-billing/dist/sdk/models/shared";

(async() => {
  const sdk = new Orb({
    security: {
      apiKeyAuth: "",
    },
  });
const externalCustomerId: string = "string";
const editCustomer: EditCustomer = {
  accountingSyncConfiguration: {
    accountingProviders: [
      {
        externalProviderId: "string",
        providerType: "string",
      },
    ],
  },
  additionalEmails: [
    "string",
  ],
  billingAddress: {},
  metadata: {},
  reportingConfiguration: {
    exempt: false,
  },
  shippingAddress: {},
  taxId: {
    country: CustomerTaxIdCountry.Hr,
    type: CustomerTaxIdType.InGst,
    value: "string",
  },
};

  const res = await sdk.customer.updateByExternalId(externalCustomerId, editCustomer);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
