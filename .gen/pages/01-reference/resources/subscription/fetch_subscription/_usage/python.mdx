```python FetchSubscription.py
import orb
from orb.models import operations, shared

s = orb.Orb(
    api_key_auth="",
)


res = s.subscription.fetch(subscription_id='string')

if res.subscription is not None:
    # handle response
    pass
```
